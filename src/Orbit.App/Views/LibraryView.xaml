<UserControl x:Class="Orbit.App.Views.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:Orbit.App.Views"
             xmlns:vm="clr-namespace:Orbit.App.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance vm:LibraryViewModel}"
             d:DesignWidth="900" d:DesignHeight="600">
    <Grid Margin="28,6,28,12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Heading}" Style="{StaticResource Text.H1}" />
                <TextBlock Style="{StaticResource Text.Caption}" Margin="0,2,0,0">
                    <Run Text="{Binding VisibleCount, Mode=OneWay}" />
                    <Run Text=" élément(s)" />
                </TextBlock>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Catégorie" Style="{StaticResource Text.Caption}"
                           VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Width="180" Style="{StaticResource ComboBox.Base}" Margin="0,0,14,0"
                          ItemsSource="{Binding Categories}"
                          SelectedItem="{Binding SelectedCategory}" />

                <TextBlock Text="Trier par" Style="{StaticResource Text.Caption}"
                           VerticalAlignment="Center" Margin="0,0,6,0" />
                <ComboBox Width="170" Style="{StaticResource ComboBox.Base}"
                          ItemsSource="{Binding SortOptions}"
                          SelectedItem="{Binding SelectedSort}">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Label}" />
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>

                <ToggleButton Margin="14,0,0,0" Cursor="Hand" Content="☑  Sélectionner"
                              IsChecked="{Binding IsSelectionMode}"
                              VerticalAlignment="Stretch" Padding="14,8"
                              Foreground="{DynamicResource Brush.Text.Primary}"
                              FontFamily="{StaticResource Font.Primary}" FontSize="13">
                    <ToggleButton.Template>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="b" Background="{DynamicResource Brush.Surface}"
                                    BorderBrush="{DynamicResource Brush.Control.Border}" BorderThickness="1"
                                    CornerRadius="9" Padding="{TemplateBinding Padding}">
                                <ContentPresenter VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="b" Property="Background" Value="{DynamicResource Brush.Accent}" />
                                    <Setter Property="Foreground" Value="{DynamicResource Brush.Text.OnAccent}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="b" Property="BorderBrush" Value="{DynamicResource Brush.Accent}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </ToggleButton.Template>
                </ToggleButton>
            </StackPanel>
        </Grid>

        <!-- Selection action bar -->
        <Border Grid.Row="1" Margin="0,0,0,12" Padding="14,10" CornerRadius="10"
                Background="{DynamicResource Brush.Accent.Soft}"
                BorderBrush="{DynamicResource Brush.Accent}" BorderThickness="1"
                Visibility="{Binding IsSelectionMode, Converter={StaticResource BoolToVisibility}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock VerticalAlignment="Center"
                           Text="{Binding SelectedCount, StringFormat='{}{0} application(s) sélectionnée(s) — cochez les cartes à retirer'}" />
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Annuler" Style="{StaticResource Button.Ghost}" Margin="0,0,10,0"
                            Command="{Binding ToggleSelectionModeCommand}" />
                    <Button Content="🗑  Supprimer la sélection" Style="{StaticResource Button.Danger}"
                            Command="{Binding DeleteSelectedCommand}" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="2">
            <ScrollViewer x:Name="Scroller" VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding ItemsView}" Margin="0,0,-14,0">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="{Binding ViewportWidth, ElementName=Scroller, Converter={StaticResource WidthToColumns}, ConverterParameter=250}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <views:AppTile />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- Empty state -->
            <Border Style="{StaticResource Card}" VerticalAlignment="Center" HorizontalAlignment="Center"
                    MaxWidth="420" Padding="28"
                    Visibility="{Binding ShowEmptyState, Converter={StaticResource BoolToVisibility}}">
                <StackPanel>
                    <TextBlock Text="Rien à afficher" Style="{StaticResource Text.H2}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="{Binding EmptyStateMessage}" Style="{StaticResource Text.Caption}"
                               TextAlignment="Center" Margin="0,8,0,16" />
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <Button Content="＋  Ajouter une application" Style="{StaticResource Button.Accent}"
                                Command="{Binding AddCommand}" />
                        <Button Content="🔍  Analyser mon PC" Style="{StaticResource Button.Base}"
                                Margin="10,0,0,0" Command="{Binding ScanForAppsCommand}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Loading -->
            <Border Background="{DynamicResource Brush.Overlay}"
                    Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}">
                <TextBlock Text="Chargement…" Foreground="White" FontSize="15"
                           HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
        </Grid>
    </Grid>
</UserControl>
